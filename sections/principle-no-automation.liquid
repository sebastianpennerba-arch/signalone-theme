<div class="section section--no-automation">
  <div class="container container--narrow">
    <div class="content-center">
      <h2 class="section-title">{{ section.settings.heading }}</h2>
      <div class="rich-text">
        {{ section.settings.content }}
      </div>
    </div>
  </div>
</div>

<style>
  .section--no-automation {
    padding: 8rem 0;
    background: var(--color-surface);
  }
  
  .content-center {
    text-align: center;
  }
  
  .content-center .section-title {
    margin-bottom: 2rem;
  }
  
  .content-center .rich-text {
    line-height: 1.8;
    color: var(--color-text-subtle);
  }
  
  .content-center .rich-text p {
    margin-bottom: 1.5rem;
  }
</style>

{% schema %}
{
  "name": "Principle No Automation",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Überschrift",
      "default": "Keine Automatisierung bedeutet Kontrolle"
    },
    {
      "type": "html",
      "id": "content",
      "label": "Inhalt",
      "default": "<p>SignalOne trifft keine Entscheidungen. Es informiert.</p><p>Die Verantwortung bleibt beim Menschen.</p>"
    }
  ],
  "presets": [
    {
      "name": "Principle No Automation"
    }
  ]
}
{% endschema %}